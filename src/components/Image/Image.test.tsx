import * as React from 'react';
import { render } from '@testing-library/react';

import { Image } from './';

import { fallbackSrc, photoSrc, loadingSrc } from '../../constants';

describe('Image', () => {});
